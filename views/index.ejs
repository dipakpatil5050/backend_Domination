<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>List of Notes - Khatabook</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-100 flex items-center justify-center h-screen">
    <div class="bg-white p-6 rounded-lg shadow-md w-96">
      <h1 class="text-3xl font-bold text-gray-800 mb-2">Khatabook</h1>
      <h2 class="text-xl font-semibold text-gray-600 mb-4">All List</h2>

      <% if (files && files.length > 0) { %>
      <ul id="notesList" class="space-y-3">
        <% files.forEach((file) => { %>
        <li
          class="flex items-center justify-between p-3 bg-blue-500 text-white rounded-lg shadow-md"
        >
          <span class="text-lg font-semibold"
            ><%= file.replace(".txt", "") %></span
          >

          <div class="flex space-x-2">
            <a href="/hisaab/<%= file %>" class="text-white text-lg">➡️</a>

            <a href="/edit/<%= file %>" class="text-white text-lg"> ✏️ </a>

            <a href="/delete/<%= file %>" class="text-white text-lg"> 🗑️ </a>
          </div>
        </li>
        <% }) %>
      </ul>
      <% } else { %>
      <p class="text-gray-500">No notes found.</p>
      <% } %>

      <a href="/create">
        <button
          class="mt-4 w-full bg-blue-500 text-white py-2 rounded-lg hover:bg-blue-600 transition"
        >
          Create Hisaab
        </button>
      </a>
    </div>
  </body>
</html>
